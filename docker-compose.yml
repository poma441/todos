version: '3'

services:
  postgres:
    image: postgres:12.12
    container_name: postgres
    restart: always
    ports:
      - 5432:5432
    env_file:
      - ./app.env
    volumes:
      - postgres:/var/lib/postgresql/data
volumes:
  postgres: